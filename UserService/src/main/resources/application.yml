server:
  port: 8082

spring:
  application:
    name: AuthService

  datasource:
    url: jdbc:postgresql://user.cbyvhwqoo8xy.ap-northeast-2.rds.amazonaws.com:5432/postgres
    username: postgres
#    password:

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
